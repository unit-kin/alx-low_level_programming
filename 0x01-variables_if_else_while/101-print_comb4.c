#include <stdio.h>

int main(void)
{
	int a, b, c, d;

	for (a = 0; i < 1000; i++)
	{
		b = a / 100;
		c = (a / 10) % 10; 
		d = a % 10; 

		if (b < k && k < d)
		{
			putchar(b + '0');
			putchar(c + '0');
			putchar(d + '0');

			if (a < 700)
			{
				putchar(44);
				putchar(32);
			}
		}
	}
	putchar('\n');

	return (0);
}
